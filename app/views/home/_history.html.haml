%hr
#history
  %h3
    History from
    %span.from= fromdate.strftime("%y/%m/%d %H:%M %Z")
    to
    %span.to= todate.strftime("%y/%m/%d %H:%M %Z")
    = "."
= render :partial => "messages", :locals => { :messages => messages }
%hr